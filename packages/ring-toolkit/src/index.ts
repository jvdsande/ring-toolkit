import { commandRegistry } from './commands/commandRegistry';

export * from '@ring-toolkit/pipeline';

export { ringToolkit } from './ringToolkit';

export const { registerCommand } = commandRegistry;
